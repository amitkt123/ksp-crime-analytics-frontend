{
	"command": "npm start",
	"buildPath": "./",
	"stack": "node20",
	"env_variables": {},
	"memory": 256,
	"scripts": {
		"predeploy": "npm install && npm run build",
		"preserve": "npm install && npm run build"
	}
}